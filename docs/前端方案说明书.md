# 教师训练智能体 · 前端方案说明书

> 面向技术交付与评审，侧重架构与数据流；安装、脚本与环境变量以仓库根目录 **README** 为准。同目录 **`前端方案说明书.html`** 为排版友好版（内嵌 SVG 流程图），便于导入 Word 或打印 PDF。教学文档分析中的思维导图与导出细节见 **`mindmap-rendering-architecture.md`**。

---

## 1. 技术选型与架构

### 1.1 技术选型（摘要）

采用 **Vue 3 + Vite** 构建无服务端渲染的单页应用；图表使用 **ECharts**（以 `vendor` 下脚本运行时加载为主，与 npm 包 `echarts` 无强制绑定）。与智能体平台的对接集中在独立的工作流客户端脚本（非 npm 包），通过浏览器全局对象向业务层暴露发送消息等能力；训练报告为直连 **HTTP JSON**（配置见 README / `.env.example`）。教学文档分析的 **PDF** 导出使用 **`html2pdf.js`**（npm 依赖）。样式采用组件作用域样式与全局样式，并做必要的移动端适配。

### 1.2 逻辑架构

应用由根壳层统一维护**当前模式**（专项一对一训练 / 课堂多生模拟 / **教学文档分析**）、**当前选中的人格**、**会话标识**以及按人格隔离的对话与情绪快照。专项与课堂复用同一套人格档案（三名学生），但对外请求走**不同通道与配置**（专项 Bot、课堂 Bot、报告与课堂主动轮询共用的 HTTP 基址等）。**训练报告仅在「课堂模拟」模块内触发**（讲台与控制台备用按钮），侧栏与其他模式不提供入口，避免重复提交。工作流返回可能是 SSE 流或非流式 JSON 字符串，前端需统一抽取「学生台词」再进入界面，避免整段 JSON 误当作对话展示。

课堂场景在业务上区分两条 outbound 路径：**教师手动发言**仍经课堂专用工作流（与其他场景密钥隔离）；**定时或调试触发的主动会话**则绕过工作流，以直连 HTTP 提交空内容并携带主动标记，且仅当响应满足约定字段时才映到头上的对话气泡。

配置在构建阶段由环境变量注入，开发时报告类接口可通过同源代理转发以降低浏览器跨域限制。

### 1.3 文档与产物

说明性网页版 `前端方案说明书.html` 内嵌 **SVG 流程图**（不依赖外网脚本即可在浏览器中查看），便于 **Word → 打开该 HTML** 或 **打印为 PDF** 再转入公文流程。文末仍保留界面截图占位，供后续贴图。

---

## 2. 核心能力（产品视角）

### 2.1 角色选择

侧栏维护三名学生的档案与当前选中项。切换学生时重建专项侧会话标识，并向工作流侧发出人格切换语义，使后续回复与新人格一致。每名学生的情绪状态分开记忆，避免来回切换时数值串扰。支持在弹窗中调节五维人格参数并实时反映到雷达预览；关闭弹窗时一般不强制重置整段训练，以减少误操作成本。

### 2.2 点名与广播（课堂）

课堂中可点选一名学生或保持「未点名」以表示面向全班。发言文案随之在「对某生」与「广播」之间区分。请求中会携带广播范围及对象标识，便于后端区分语境。视觉上可将同一段全班回复集中到一名代表位置展示，以免重复三条相同气泡；主动轮询默认也采用代表位展示。

### 2.3 对话形态

**专项**：教师输入与快捷教学策略标签一并发出；回复经工作流以流式为主返回，前端归并为完整一句学生话并写入对话区历史按人格分桶。**课堂手动**：教师输入经课堂工作流返回，结果进入事件流与头顶气泡。**课堂主动**：仅直连 HTTP，空正文 + 主动标记；仅当响应含约定的主动话术字段且未命中「无内容」占位时更新气泡，其余响应静默。

### 2.4 情绪可视化

专项侧以愉悦、激活、焦虑等维度展示当前状态与时间序列，并可在工作流回包中解析情绪相关调试信息以刷新看板；教师操作标签也可触发轻微的本地情绪扰动作为辅助反馈。课堂侧对三名学生分别维护简化情绪标量并在控制台区域呈现。训练报告中若带有调试情绪块，以独立小节展示，与对话气泡解耦。

### 2.5 训练报告

在达到最低对话量等前置条件后，由 **课堂模拟** 内的「结束 · 生成报告」触发（根组件 `endSession`）：先展示加载层（课堂模式下可挂载在课堂主区，侧栏仍可切换模式），再向报告接口发起带「评估」语义的 JSON 请求；返回体经解包后提取结构化评估块与可选调试块，用于概况、维度得分、改进建议等区块；若后端未提供结构化评估，则回退到基于本地历史的启发式打分与文案。能力雷达与条形图优先采用后端大类得分；弹窗内可提供基于评估结果的「专项训练」快捷跳转（关闭报告后进入专项模式并选中对应人格）。

---

## 3. 与后端协作要点

### 3.1 通道划分

| 业务 | 通道概要 |
|------|-----------|
| 专项对话 | 智能体 SSE 工作流，专用应用密钥 |
| 课堂手动 | 同协议，独立课堂应用密钥 |
| 教学文档分析 | 上传后走 SSE 工作流；可用独立 `VITE_DOC_BOT_APP_KEY`，请求体以文件 URL 等为主（详见 `.env.example`） |
| 课堂主动 | 直连类 Chat Completions 的 HTTP，请求体含主动标记与空消息 |
| 训练报告 | 同源 HTTP 配置，请求体含评估标记；**触发入口仅在课堂模拟组件内** |

### 3.2 请求与响应（概念）

工作流侧为 POST + 流式响应，正文包含会话标识、内容、应用密钥及自定义业务字段（如策略标签、模型名、课堂范围、文档 URL 等）。报告与课堂主动轮询为 POST + JSON，使用 API 密钥或 Bearer 头（以部署配置为准）。前端需兼容「流式增量」与「单次 JSON 字符串」两种回复形态，并从中解析助手话术；课堂主动轮询另有一套「是否展示」的判定，与手动路径不同。

### 3.3 状态与并发

根层持有模式、当前人格、会话 id、分人格对话与情绪缓存；工作流脚本可通过约定好的全局对象读取扩展字段以供报告等使用。课堂侧对工作流与主动轮询使用简单忙标志；**生成训练报告期间**以 `reportGenerationBusy` 禁用课堂内报告按钮并协调加载层显示，避免重复提交。

### 3.4 异常与降级

网络失败、跨域或 HTTP 错误时，专项与课堂倾向于在对话区或课堂事件区给出可读提示并记录控制台标签，不阻断整页。未配置课堂密钥则跳过课堂请求。主动轮询在地址缺失或非成功响应时清空气泡并记录原因。报告请求失败或返回不可解析时，关闭加载层并尽量依赖本地历史打开简版报告；密钥落在前端 bundle 中的部署需知悉安全风险，生产建议走网关或代理。

---

## 4. 配图占位（界面截图）

以下由实施或产品补图即可。

- **图 A**：应用总览（侧栏 + 专项主区 + 三模式切换）。
- **图 B**：课堂场景（座位、气泡、输入、主动轮询与 **课堂内** 报告入口）。
- **图 C**：报告加载层与报告弹窗。
- **图 D**（可选）：教学文档分析（上传、弹窗内思维导图与导出）。
- **图 E**（可选）：浏览器开发者工具中各通道日志过滤示意。

---

## 5. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.2 | 2026-04-18 | 对齐当前实现：三模式、训练报告仅课堂内触发；补充文档分析与 PDF 依赖说明；配图占位更新 |
| 1.1 | 2026-03-25 | 压缩实现细节；增加网页版与 SVG 流程图入口 |
| 1.0 | 2026-03-25 | 初稿 |
